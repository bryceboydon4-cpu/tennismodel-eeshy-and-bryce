from collections import defaultdict

surface_elos = {
    "Hard": defaultdict(lambda: 1500),
    "Clay": defaultdict(lambda: 1500),
    "Grass": defaultdict(lambda: 1500),
    "Carpet": defaultdict(lambda: 1500)
}